matrix:
  include:
    - language: java
      jdk: openjdk8
      before_install:
      - cd coding && chmod +x mvnw
      script:
        - ./mvnw test
    - language: java
      jdk: openjdk8
      before_install:
      - cd gamification && chmod +x mvnw
      script:
        - ./mvnw test
    - language: node_js
      node_js:
        - "node"
      before_script:
        - cd web && npm install
      cache:
        directories:
          - "node_modules"
      script:
        - npm run test_headless
    #- language: android
    #  dist: trusty
    #  android:
    #    components:
    #      - build-tools-29.0.2
    #      - android-28
    #  before_script:
    #    - cd mobile
    #  script:
    #    - ./gradlew build connectedCheck
    
notifications:
  email: false